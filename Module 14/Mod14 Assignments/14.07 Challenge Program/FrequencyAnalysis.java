import java.io.*;
import java.util.*;

public class FrequencyAnalysis {

	static ArrayList<String> alphabet = new ArrayList<String>();
	static ArrayList<Integer> characters = new ArrayList<Integer>();
	static int count = 0;

	alphabet.add("a");

	public static void frequency(File file) {
		Scanner in = new Scanner(System.in);

		while(in.hasNextLine()) {
			String info = in.nextLine();
			info = info.toLowerCase();
			int num = 0;
			int index = 0;

			for(int i = 0; j < info.length(); i ++) {
				if
			}
		}
	}
}